apiVersion: v1
kind: Service
metadata:
  name: pulseforge
  namespace: pulseforge
spec:
  selector:
    app.kubernetes.io/name: pulseforge
  ports:
    - name: http
      port: 8080
      targetPort: http